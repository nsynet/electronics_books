/********************************************************************
                            汇诚科技
					  http://www.ourhc.cn
*********************************************************************/
#include<reg52.h>  	       //库文件
#define uchar unsigned char//宏定义无符号字符型
#define uint unsigned int  //宏定义无符号整型
/********************************************************************
                            初始定义
*********************************************************************/

sbit P2_0=P2^0; //定义按键的输入端（为单片机P2口的P2.1按键）
sbit P2_1=P2^1; //定义按键的输入端（为单片机P2口的P2.1按键）
sbit P2_2=P2^2; //定义按键的输入端（为单片机P2口的P2.1按键）
sbit P2_3=P2^3; //定义按键的输入端（为单片机P2口的P2.1按键）
        
/********************************************************************
                            主函数
*********************************************************************/
void main()
{

	while(1)
	{
	if(P2_0==0)P0=0X55;	//如果P2.0按键按下，P0口输出01010101
	if(P2_1==0)P0=0XAA;	//如果P2.1按键按下，P0口输出10101010
	if(P2_2==0)P0=0X0F;	//如果P2.2按键按下，P0口输出00001111
	if(P2_3==0)P0=0XF0;	//如果P2.3按键按下，P0口输出11110000
	}
}
	
/********************************************************************
                              结束
*********************************************************************/
